@using Servicify.Models
@model Servicify.Core.Organization
@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}
<link rel="stylesheet" href="~/css/organization/viewOne.css">
<div class="organization-container">
    <div class="organization-details">
        <h2>@Model.Name</h2>
        <p>@Model.Description</p>
    </div>
    <div class="organization-buttons">
        <button class="subscribe-btn">Подписаться на организацию</button>
        <button class="contact-btn">Написать исполнителю</button>
    </div>
</div>
<div class="service-list">
    @*@foreach (var service in Model)
    {
        var serviceViewModel = new ServiceViewModel
        {
            Name = service.Name,
            Duration = service.Duration,
            Cost = service.Cost
        };
        @await Html.PartialAsync("_ServiceCard", serviceViewModel)
    }*@
    @await Html.PartialAsync("_ServiceCard", new ServiceViewModel(1, "Тест", "120", 300f))
    @await Html.PartialAsync("_ServiceCard", new ServiceViewModel(1, "Тест", "120", 300f))
    @await Html.PartialAsync("_ServiceCard", new ServiceViewModel(1, "Тест", "120", 300f))
</div>
